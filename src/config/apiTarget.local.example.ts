/**
 * Copy to apiTarget.local.ts (gitignored) to override without editing apiTarget.ts
 *
 * export const API_TARGET = 'local' as const;
 * export const RAILWAY_API_URL = 'https://jean-production-dad4.up.railway.app';
 */
